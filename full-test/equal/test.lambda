extern print_bool(Bool) : Unit;
extern input_int() : Int;

func same(x, y) {
    return x == y;
}

func main() {
    x <- input_int();
    y <- input_int();
    s <- same(x, y);
    print_bool(!(x != y && !s));
}
